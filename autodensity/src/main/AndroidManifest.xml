<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tang.autodensity" >
    <application>
        <provider
            android:name=".DensityProvider"
            android:authorities="${applicationId}.density-init-provider"
            android:exported="false"/>
    </application>
</manifest>
